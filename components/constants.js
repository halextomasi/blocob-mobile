import { Easing } from 'react-native';

// Layout constants
export const center = {
    top: 8,
    left: 30,
};

export const topCenter = {
    top: -90,
    left: 30,
};

export const topLeft = {
    top: -60,
    left: -40,
};

export const topRight = {
    top: -60,
    left: 100,
};

// Style constants
export const bigBubbleSize = 90;
export const bubbleColor = 'rgb(40, 155, 238)';